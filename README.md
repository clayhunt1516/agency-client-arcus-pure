# Arcus Pure — Company Brain (Git layer)

This is the agency-side Git layer of the Arcus Pure Company Brain. Arcus
Pure is a whole-space disinfection company based in Cedar City, Utah.

The Git layer holds framework-side scaffolding: orientation `CLAUDE.md`
files, accounting SOPs, report specifications, testing structure,
archived content. **Client-knowledge content lives in Notion** — see the
root `CLAUDE.md` for the Notion pointers section with stable page and
database IDs.

For AI agent orientation: start at `CLAUDE.md`.

For framework version and deployment history: see `DEPLOYMENT.md`.

For the populated deployment input that seeded this repo: see
`_deployment-input.md` (gitignored; locally present).
